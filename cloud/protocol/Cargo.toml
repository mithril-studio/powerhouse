[package]
name = "powerhouse-cloud-protocol"
version = "0.1.0"
edition = "2021"
description = "Versioned contracts shared by the Powerhouse desktop and the in-VM cloud runner."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
